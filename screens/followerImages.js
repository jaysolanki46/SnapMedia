import * as React from 'react';
import {StyleSheet, Text, View, ActivityIndicator, FlatList} from 'react-native';

export default class followerImages extends React.Component {

    render() {
        return(
            <View>
                <Text>Hellow</Text>
            </View>
        );
    }
}